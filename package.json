{
  "name": "kai-trad-pwa",
  "version": "1.6.2-phone-flat",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && wrangler dev",
    "test": "node --test engine.test.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
